APP_ENV=demo
PORT=8000
CYCLONE_DATA_INDEX=data/processed/index.csv
CYCLONE_MODEL_MANIFEST=models/active-model.json
# Comma-separated exact frontend origins only when running cross-origin.
ALLOWED_ORIGINS=
